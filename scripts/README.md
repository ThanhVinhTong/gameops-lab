# Scripts

This directory is reserved for small automation helpers introduced in later
phases. The repository foundation does not provide runnable project scripts.

AWS, SAM, `samlocal`, and LocalStack API scripts must not be introduced or run
before the Stage F local integration work.
